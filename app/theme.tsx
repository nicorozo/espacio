import { createTheme } from "@mui/material";

export const myTheme = createTheme({
  colorSchemes: {
    mode: 'dark',{
      primary:{
        main: '#0a0a0a',
        secondary:'#ededed'
      }
    }
  },
});
